<UserControl x:Class="SmartRenamer.Controls.WorkflowCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border BorderBrush="#D9D9D9"
            BorderThickness="1"
            CornerRadius="8"
            Padding="15"
            Margin="10">

        <DockPanel>

            <!-- Header -->

            <TextBlock DockPanel.Dock="Top"
                       Text="Options"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0,0,0,15"/>

            <!-- Options -->

            <ItemsControl ItemsSource="{Binding Pipeline.Steps}">

                <ItemsControl.ItemsPanel>

                    <ItemsPanelTemplate>

                        <StackPanel/>

                    </ItemsPanelTemplate>

                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>

                    <DataTemplate>

                        <Border Margin="0,0,0,8"
            Padding="10"
            CornerRadius="6"
            BorderBrush="#CCCCCC"
            BorderThickness="1">

                            <StackPanel Orientation="Horizontal">

                                <TextBlock Text="{Binding Icon}"
                       FontSize="16"
                       Margin="0,0,8,0"/>

                                <StackPanel>

                                    <TextBlock Text="{Binding Name}"
                           FontWeight="SemiBold"/>

                                    <TextBlock Text="{Binding Description}"
                           FontSize="11"
                           Foreground="Gray"/>

                                </StackPanel>

                            </StackPanel>

                        </Border>

                    </DataTemplate>

                </ItemsControl.ItemTemplate>

            </ItemsControl>

        </DockPanel>

    </Border>

</UserControl>