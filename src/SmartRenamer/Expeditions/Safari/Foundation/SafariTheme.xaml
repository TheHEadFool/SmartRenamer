    <ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===================================================== -->
    <!-- Colors -->
    <!-- ===================================================== -->

    <SolidColorBrush x:Key="BackgroundBrush"
                     Color="#ECE8DE"/>

    <SolidColorBrush x:Key="PrimaryBrush"
                     Color="#28432A"/>

    <SolidColorBrush x:Key="SecondaryBrush"
                     Color="#556B2F"/>

    <SolidColorBrush x:Key="CardBrush"
                     Color="#F8F4EA"/>

    <SolidColorBrush x:Key="BorderBrush"
                     Color="#D6CDBA"/>

    <SolidColorBrush x:Key="SecondaryTextBrush"
                     Color="#6D6A5F"/>

    <SolidColorBrush x:Key="AccentBrush"
                     Color="#B88935"/>

    <!-- ===================================================== -->
    <!-- Card Style -->
    <!-- ===================================================== -->

    <Style x:Key="CardBorder"
           TargetType="Border">

        <Setter Property="Background"
                Value="{StaticResource CardBrush}" />

        <Setter Property="CornerRadius"
                Value="16"/>

        <Setter Property="Padding"
                Value="18"/>

        <Setter Property="BorderBrush"
                Value="{StaticResource BorderBrush}" />

        <Setter Property="BorderThickness"
                Value="1"/>

        <Setter Property="Margin"
                Value="0"/>

    </Style>


    <!-- ===================================================== -->
    <!-- Conversation Presentation -->
    <!-- ===================================================== -->
    <!--
         Scout owns the ConversationPanel layout.
         Safari owns the visual presentation of that conversation.

         These resources allow the same Scout conversation layout
         to be presented differently by different Expeditions.
    -->

    <SolidColorBrush x:Key="ConversationHeaderBrush"
                     Color="#28432A"/>

    <SolidColorBrush x:Key="ConversationHeaderTextBrush"
                     Color="#FFFFFF"/>

    <SolidColorBrush x:Key="ConversationSubtitleBrush"
                     Color="#D8C38A"/>

    <SolidColorBrush x:Key="ConversationBubbleBrush"
                 Color="#E3E6D2"/>

    <SolidColorBrush x:Key="ConversationBorderBrush"
                     Color="#D8DEE8"/>

    <SolidColorBrush x:Key="ConversationInputBrush"
                     Color="#FFFFFF"/>

    <SolidColorBrush x:Key="ConversationSendBrush"
                     Color="#B88935"/>

    <SolidColorBrush x:Key="ConversationSendBorderBrush"
                     Color="#8F6825"/>

    <SolidColorBrush x:Key="ConversationSendHoverBrush"
                     Color="#C99D4D"/>

    <SolidColorBrush x:Key="ConversationSendPressedBrush"
                     Color="#76531D"/>

    <SolidColorBrush x:Key="ObservationSurfaceBrush"
                 Color="#F0F1E7"/>

    <!-- ===================================================== -->
    <!-- Observation Presentation -->
    <!-- ===================================================== -->

    <SolidColorBrush x:Key="ObservationCardBrush"
                     Color="#E3E6D2"/>

    <SolidColorBrush x:Key="ObservationBorderBrush"
                     Color="#D6CDBA"/>

    <!-- ===================================================== -->
    <!-- Observation Button -->
    <!-- ===================================================== -->

    <Style x:Key="ObservationButtonStyle"
           TargetType="Button">

        <Setter Property="Background"
                Value="{StaticResource PrimaryBrush}"/>

        <Setter Property="Foreground"
                Value="White"/>

        <Setter Property="BorderBrush"
                Value="{StaticResource AccentBrush}"/>

        <Setter Property="BorderThickness"
                Value="1"/>

        <Setter Property="FontWeight"
                Value="SemiBold"/>

        <Setter Property="Cursor"
                Value="Hand"/>

    </Style>

</ResourceDictionary>